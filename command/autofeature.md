---
description: Apply the default adaptive workflow with explicit end-to-end autonomy.
agent: build
---

Implement this feature end to end:

$ARGUMENTS

Follow the global adaptive Superpowers workflow used by normal feature prompts. This command explicitly requests end-to-end autonomy; it does not select a separate process. Inspect project instructions and relevant code, front-load foreseeable material questions, and proceed without an interview or manufactured design gate when the request and architecture already determine the solution. When a material design choice exists, request one cohesive design approval. Ask again during implementation only if an unexpected material product, security, compatibility, data-loss, cost, or scope decision cannot be inferred safely.

For substantive work in a Git repository, use a worktree unless a valid policy exception applies and report that exception. Once the request or design is accepted, write any useful design record and a durable detailed plan under `docs/superpowers/plans/`, choose the execution method internally, and start implementation in the same session. Do not ask for written-spec review, plan review, an execution-mode choice, routine task approval, or permission to continue. Maintain a separate, reasonably scaled dynamic execution tree with a feature root, useful milestones, and current or near-term children rather than copying every plan step. Keep status, active work, owner, priority, and real blockers current.

Keep the main model responsible for architecture and integration. It resolves routine subagent questions itself from the accepted design, repository evidence, and safe reversible judgment. Delegate narrowly bounded research, isolated implementation, or focused verification when useful for context isolation or speed; do not overlap edits or dispatch automatic reviewers. Run proportional checks covering the changed dependency surface. Do not offer or launch browser tooling by default. For user-facing changes, finish with the launch command, URL, required test state, concise manual browser checklist, changed files, and known gaps. Do not commit, push, publish, deploy, or alter unrelated work unless explicitly requested.
