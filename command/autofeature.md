---
description: Apply the default focused-delivery workflow with explicit end-to-end autonomy.
agent: build
---

Implement this substantive feature end to end:

$ARGUMENTS

Invoke `focused-delivery` before planning or implementation. Inspect project instructions and relevant context, then ask all foreseeable independent material questions in one concise batch. Present the at-most-ten-line goal contract, cohesive design, and comprehensive implementation plan together for one approval. Do not request section approval, written-spec approval, separate plan approval, execution-mode selection, or routine permission to continue.

After approval, execute autonomously in independently useful, browser-testable or releasable vertical slices. Use a Git worktree when the global policy requires it. Keep the main model responsible for architecture and integration; delegate only bounded non-overlapping work.

At each vertical-slice boundary run proportionate verification, dispatch at most one reviewer whose prompt requires invoking `scope-bounded-review`, consolidate blockers into one fix wave, and perform one scoped re-review. A third round or scope expansion requires an explicit user decision.

Verify relevant runnable provenance before saying `ready`. Leave visual and browser testing to the user unless they explicitly ask the agent to perform it. Finish with the launch command, URL, required state, concise browser checklist, changed files, automated evidence, known gaps, and deferred risks. Do not commit, push, merge, create a PR, deploy, release, discard, or clean up unless explicitly requested.
